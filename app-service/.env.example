PORT=4003

# Database Configuration (for public key verification)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=zerotrust
DB_USER=postgres
DB_PASSWORD=postgres

# Note: HMAC and JWT secrets are managed by Vault
# Do not add GATEWAY_HMAC_SECRET or JWT_SECRET here
